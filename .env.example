# Server Configuration
PORT=3001
NODE_ENV=development

# Database (Phase 2 - Railway provides this automatically)
# DATABASE_URL=postgresql://user:password@host:5432/database

# JWT Secret (Phase 2 - for user authentication)
# JWT_SECRET=your-super-secret-key-change-this-in-production

# CORS (for development, Railway handles this in production)
# ALLOWED_ORIGINS=http://localhost:3001

# Game Configuration
ROOM_EXPIRY_HOURS=1
MAX_ROOMS=1000
